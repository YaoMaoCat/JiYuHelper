<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JiYuHelper.Views.HelpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JiYuHelper.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="16" MinWidth="760">

            <StackPanel Spacing="4">
                <TextBlock Text="帮助" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="JiYuHelper 使用指南" Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- 新手引导 (仅新手模式显示) -->
            <Border Tag="novice" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="快速使用" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
1. 打开「扫描」页，点击「扫描教师机」，在列表中选择教师机；
2. 打开「控制」页，勾选需要的功能，点击「注入并启用」；
3. 打开「攻击」页，选择模式并点击「开始攻击」；
4. 在「日志」页查看运行状态。
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- 快速上手 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="快速上手" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}"
                               LineHeight="24">
1. 进入「扫描」页，点击「扫描教师机」（默认网段扫描，可选组播扫描），或手动添加教师机 IP；
2. 在列表中点击选中目标教师机；
3. 进入「控制」页，勾选需要的 Hook 功能，点击「注入并启用」（首次会提示提权重启）；
4. 进入「攻击」页，选择攻击模式，设置线程数与持续时间，点击「开始攻击」；
5. 在「日志」页查看实时攻击状态、注入状态与拦截事件。
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- 控制页说明 -->
            <Border Tag="tech" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="控制页说明" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
控制页用于向本机极域进程注入 Hook（bypass_main.dll → StudentMain.exe，bypass_master.dll → MasterHelper.exe）。勾选功能后点击「注入并启用」，程序会先写入 hook.cfg 配置，再注入 DLL；开启「自动重注入」后，极域进程重启会自动再次注入。
                    </TextBlock>

                    <TextBlock Text="功能分组" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold"/>
                    <StackPanel Spacing="6">
                        <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
· 远程控制拦截（脱控）：远程输入拦截（SendInput/BlockInput）、输入锁定放行、进程操作守护（进程/关机指令）、进程终止能力屏蔽、设备过滤屏蔽（USB/CD/程序执行）、网络仿真屏蔽（断网模拟指令，MasterHelper）；
                        </TextBlock>
                        <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
· 界面与进程（窗口化）：置顶窗口剥离（WS_EX_TOPMOST）、焦点锁定拦截（黑屏窗口无法抢焦点）、应用列表屏蔽、进程列表屏蔽；
                        </TextBlock>
                        <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
· 屏幕监控：屏幕假屏（截屏注入 screen.png）、屏幕捕获屏蔽（BitBlt 假画面）、黑屏监控（黑屏自动窗口化）；
                        </TextBlock>
                        <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
· 输入：键盘钩子绕过（低级键鼠钩子替换为空钩子）。
                        </TextBlock>
                    </StackPanel>

                    <TextBlock Text="注意事项" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold"/>
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
· 极域为 32 位软件，必须使用 x86 版本的程序注入（MasterHelper 为 SYSTEM 进程，注入需要管理员权限）；
· 功能开关修改后需重新注入生效（勾选即写 hook.cfg）；
· 屏幕假屏 / 捕获屏蔽需要程序目录下的 screen.png 占位图；
· 拦截事件（BLOCKED）与 Hook 状态经命名管道回传，统一显示在「日志」页。
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- 攻击模式说明 -->
            <Border Tag="tech" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="攻击模式说明" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <StackPanel Spacing="10">
                        <StackPanel Spacing="4">
                            <TextBlock Text="FILESUBMIT 协议崩溃" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold"/>
                            <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
利用教师端文件提交命令处理中的无界 wchar 拷贝 (TeacherMain sub_5661e0, 无 SEH 保护) 触发栈缓冲区溢出, 进程级崩溃。建议多线程持续攻击。
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="ANSWERSHEET 协议崩溃" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold"/>
                            <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
与 FILESUBMIT 共用同一崩溃点 (sub_5661e0 case2), 实测触发更快。
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="UPFB 上传崩溃" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold"/>
                            <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
先发送 IFPU 建立上传会话 (目标路径可含任意路径, 实测无路径过滤), 再发送 UPFB 数据块触发进程崩溃。实测写入内容 (含可控头部字段 + 载荷前段) 会先落盘, 随后教师端进程必然退出。
                            </TextBlock>
                            <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
技术细节: IFPU.fileSize 字段控制写入偏移 (实测 50/60/100 精确命中), UPFB 长度字段控制写入量 (len-8 字节), memcpy 无上限是崩溃根因 (LibMiniMedia10 sub_10006460), 崩溃写 0x7D 野指针与异步写完成回调有关。单发即崩, 无法多次写入。
                            </TextBlock>
                        </StackPanel>

                        <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
其他攻击模式 (QRMS/RFSA/QDAT/BLKC/PGIJ 堆溢出) 已在测试中验证为连接级杀伤 (仅影响单个连接, 进程存活), 目前在界面中隐藏。
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 参数说明 -->
            <Border Tag="tech" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="参数说明" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
· 线程数: 并发攻击连接数, 建议 10-100, 越大越容易触发崩溃;
· 持续时间: 攻击总时长 (秒), 到期自动停止;
· 崩溃信号: 观察到连接被重置/异常关闭的次数, 累计出现即可能已崩。
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- 关于 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="关于" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" LineHeight="24">
JiYuHelper — 极域电子教室教师端安全研究工具

分析对象: 极域课堂管理系统软件 v6.0 2021 豪华版 (CMPC 2.07.0.17364)
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
